import 'package:flutter/material.dart';

class MachineListScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Machine List'),
      ),
      body: Center(
        child: Text('Machine List Screen'),
      ),
    );
  }
}